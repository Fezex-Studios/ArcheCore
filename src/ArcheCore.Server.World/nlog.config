<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true">

    <targets>
        <target xsi:type="ColoredConsole" name="console"
                layout="${time} [${level:uppercase=true}] [${logger:shortName=true}] ${message} ${exception:format=tostring}">
            <highlight-row condition="level == LogLevel.Fatal" foregroundColor="Red" />
            <highlight-row condition="level == LogLevel.Error" foregroundColor="DarkRed" />
            <highlight-row condition="level == LogLevel.Warn"  foregroundColor="Yellow" />
            <highlight-row condition="level == LogLevel.Info"  foregroundColor="White" />
            <highlight-row condition="level == LogLevel.Debug" foregroundColor="Gray" />
            <highlight-row condition="level == LogLevel.Trace" foregroundColor="DarkGray" />
        </target>

        <target xsi:type="File" name="file"
                fileName="logs/worldserver-${shortdate}.log"
                layout="${longdate} [${level:uppercase=true}] [${logger}] ${message} ${exception:format=tostring}"
                archiveEvery="Day"
                maxArchiveFiles="7" />
    </targets>

    <rules>
        <!-- Kill EF Core / Microsoft / System spam -->
        <logger name="Microsoft.EntityFrameworkCore.*" maxlevel="Off" final="true" />
        <logger name="Microsoft.*"                     minlevel="Warn" writeTo="console,file" final="true" />
        <logger name="System.*"                        minlevel="Warn" writeTo="console,file" final="true" />

        <!-- Game server. final="true" stops the same line ALSO matching the
             catch-all rule below - without it every ArcheCore Info+ line was
             written twice. -->
        <logger name="ArcheCore.*" minlevel="Debug" writeTo="console,file" final="true" />

        <!-- Fallback for anything else (LiteNetLib, MoonSharp, and our classes
             that live outside the ArcheCore.* namespace, e.g. PersistenceClient
             and QuestManager) -->
        <logger name="*" minlevel="Info" writeTo="console,file" />
    </rules>

</nlog>
